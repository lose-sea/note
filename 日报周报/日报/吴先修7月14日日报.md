# 7月14日日报

## 本日学习内容

1. 完成了网易云音乐的歌单详情界面的布局和交互
2. 修改了原来在首页的自定义Cell,添加了歌单属性, 并实现了点击跳转歌单详情界面的功能
3. 初步了解了网络请求的相关知识URL,通过Open-Meteo提供的API实现通过城市名查找经纬度的示例了解网络请求的步骤和实现

## 今日算法题

### 题目1: [40. 组合总和 II](https://leetcode.cn/problems/combination-sum-ii/)

![image-20260617213632779](/Users/lose_sea/Desktop/pintures/image-20260617213632779.png)

```objc
class Solution {
public: 

    vector<int> path; 
    vector<vector<int>> result; 
    void backTracking(vector<int>& candidates, int target, int startIndex, int currsum) {
        if (currsum == target) {
            result.push_back(path); 
        }  
        unordered_set<int> uset; 
        for (int i = startIndex; i < candidates.size(); i++) {
            if (currsum + candidates[i] > target) {
                break; 
            }  
            if (uset.find(candidates[i]) != uset.end()) {
                continue; 
            } 
            uset.insert(candidates[i]); 
            path.push_back(candidates[i]); 
            backTracking(candidates, target, i + 1, currsum + candidates[i]); 
            path.pop_back(); 
        }
    }
    vector<vector<int>> combinationSum2(vector<int>& candidates, int target) { 
        sort(candidates.begin(), candidates.end()); 
        backTracking(candidates, target, 0, 0); 
        return result; 
    }
};
```

## 本日遇到的问题

1. 对网络请求的相关知识点(中文的URL编码, 请求地址的创建)理解不清
2. 仿写仿写通过城市名获取城市所在省和经纬度的示例 未完成

## 明日学习计划

1. 继续仿写通过城市名获取城市所在省和经纬度的示例, 熟悉网络请求的相关知识

