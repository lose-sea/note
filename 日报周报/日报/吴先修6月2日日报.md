# 6月2日日报

## 本日学习内容

1. share 仿写 
2. 使用自定义cell实现了个人信息界面的布局 

## 今日算法题

### 题目1: [98. 验证二叉搜索树](https://leetcode.cn/problems/validate-binary-search-tree/) 

![image-20260602215002556](/Users/lose_sea/Desktop/pintures/image-20260602215002556.png)

```objc
class Solution {
public:

    bool judge = true; 
    TreeNode* pre = nullptr; 
    void traversal(TreeNode* root) {
        if (judge == false) {
            return; 
        } 
        if (root == nullptr) {
            return; 
        }
        traversal(root->left); 
        if (pre != nullptr && root->val <= pre->val) {
            judge = false; 
            return; 
        } 
        pre = root; 
        traversal(root->right); 
    }
    bool isValidBST(TreeNode* root) {
        if (root == nullptr) {
            return true; 
        } 
        traversal(root);
        return judge; 
    }
};
```

## 本日遇到的问题

1. 在设置个人关注界面的时候, 对按钮的边框的属性设置不熟悉(圆形边框 , 边框颜色) 
2. 在练习 MVC 时, 在 view 中使用Masonry 进行布局, 但是需要用的控件的width 和 height 属性, 但是访问值为 0 ,使用 layoutIfNeeded 强制刷新布局也没有用

## 明日学习计划

1. share仿写
2. 在仿写界面的过程中回顾复习自己之前学习过的掌握不过熟练的知识点

