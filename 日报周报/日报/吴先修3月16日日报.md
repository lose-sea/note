## 本日学习内容

1. 详细回顾了NSArray 和 NSMutbleArray的相关操作 (创建, 访问, 追加, 查找, 对集合整体调用方法, 排序, 使用枚举器遍历) 
1. 了解isa指针, 实例对象, 类对象, 元类对象的关系
1. 字符串的存储位置

## 今日算法题

## 题目: [24. 两两交换链表中的节点](https://leetcode.cn/problems/swap-nodes-in-pairs/)

![image-20260316201251918](/Users/lose_sea/Library/Application Support/typora-user-images/image-20260316201251918.png)

```c++
// 递归
class Solution {
public:
    ListNode* swapPairs(ListNode* head) {
        if (head == nullptr || head->next == nullptr) {
            return head; 
        }  
        ListNode* curr = head; 
        ListNode* next = curr->next; 
        head = next;  
        curr->next = swapPairs(next->next);  
        head->next = curr;  
        return head; 
    }
}; 
// 迭代
class Solution {
public:
    ListNode* swapPairs(ListNode* head) {
        if (head == nullptr || head->next == nullptr) {
            return head; 
        }  
        ListNode* dummy = new ListNode(0);  
        dummy->next = head; 
        ListNode* prev = dummy;  
        while (prev->next != nullptr && prev->next->next != nullptr) {
            ListNode* curr = prev->next;  
            ListNode* next = curr->next;   
            ListNode* temp = next->next;  

            prev->next = next; 
            next->next = curr; 
            curr->next = temp;  
            prev = curr; 
        } 
        return dummy->next; 
    }
}; 
```



## 今日遇到的问题

1. 对于stringWithString  和 stringWithFormat 方法创建出来的字符串的存储位置理解不清 
1. copy, mutableCopy, 深浅复制的关系理解不清 

## 明日学习计划

1. 详细回顾 NSSet 的各种方法 (创建, 添加, 获取元素个数, 判断是否包含某元素, 交际 ,并集, 差集) 

   ​    