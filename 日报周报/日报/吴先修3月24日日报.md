## 本日学习内容

1. 字符串(NSString, NSMutableString) 的基本操作操作回顾练习 (创建, 追加, 截取)
2. 数组(NSArry 和 NSMutableArray) 的基本操作回顾练习 (创建, 追加, 访问, 查找, 插入, 对集合整体调用方法 (makeObjectsPerformSelector: , enumerateObjectsAtIndexes: option: usingBlock: )); 

## 今日算法题

### 题目1: [15. 三数之和](https://leetcode.cn/problems/3sum/)

![image-20260324215945213](/Users/lose_sea/Library/Application Support/typora-user-images/image-20260324215945213.png)

```objc
// 哈希表
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> result; 
        sort(nums.begin(), nums.end()); 
        for (int i = 0; i < nums.size(); i++) {
            if (nums[i] > 0) {
                break; 
            } 
            if (i > 0 && nums[i] == nums[i - 1]) {
                continue; 
            }
            unordered_set<int> st; 
            for (int k = i + 1; k < nums.size(); k++) {
                if (k > i + 2 && nums[k] == nums[k - 1] && nums[k] == nums[k - 2]) {
                    continue; 
                } 
                int target = -nums[i] - nums[k]; 
                if (st.contains(target)) {
                    result.push_back({nums[i], nums[k], target}); 
                    st.erase(target); 
                } else {
                    st.insert(nums[k]); 
                } 
            } 
        } 
        return result; 
    }
}; 

// 双指针
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> result; 
        sort(nums.begin(), nums.end()); 
        int left = 0; 
        int right = nums.size() - 1; 
        for (int i = 0; i < nums.size() - 2; i++) {
            if (nums[i] > 0) {
                break; 
            }  
            if (i > 0 && nums[i] == nums[i - 1]) {
                continue; 
            }
            int left = i + 1; 
            int right = nums.size() - 1; 
            while (left < right) {
                if (nums[i] + nums[left] + nums[right] < 0) {  
                    left++; 
                } else if (nums[i] + nums[left] + nums[right] > 0) {
                    right--;  
                } else {
                    result.push_back({nums[i], nums[left], nums[right]});  
                    while (left < right && nums[left] == nums[left + 1]) {
                        left++; 
                    } 
                    while (left < right && nums[right] == nums[right - 1]) {
                        right--; 
                    } 
                    left++; 
                    right--; 
                }
            }
        }
        return result; 
    }
};
```



## 本日遇到的问题

1. NSArray 在一定范围内查找某个元素返回的是该元素在整个数组中的索引, 不是在该范围的相对索引, 如果没有找到则返回NSNotFound 
1. IndexSet, NSMakeRange的在方法中作为参数的使用

## 明日学习计划

1. 复习NSSet, NSDictionary 的相关操作
1. 将各种方法的重写作一复习练习(委托, isEqual, 单例, init, hash)

