# 4月27日日报

## 本日学习内容

1. 回顾并重新解决了在反向传值的过程中出现的 如果从 B 视图的 UITextField 传回 A 视图的 label 显示, 如果是 A 视图的NSString 属性, 会导致无法正常显示 
2. 在传值后再次切换视图会导致原本内容清空, 如果需要保持,就需要将正向传值和反向传值结合,再次切换到视图 B 的时候, 向B视图的 UITextField 传入 A 的Label 的内容, 使两个视图的内容保持同步
3. 复习了UITableView的相关操作并学习UITableView拓展操作(批量删除, 头部视差效果, 编辑cell 的内容)

### 题目1: [25. K 个一组翻转链表](https://leetcode.cn/problems/reverse-nodes-in-k-group/) 

![image-20260427171328612](/Users/lose_sea/Desktop/pintures/image-20260427171328612.png)

```objc
// 迭代
#define ll long long 
class Solution {
public: 
    ListNode* reverseKGroup(ListNode* head, int k) {
        ll n = 0; 
        for (ListNode* cur = head; cur; cur = cur->next) {
            n++; 
        } 
        ListNode* dummy = new ListNode(0, head); 
        ListNode* p0 = dummy; 
        ListNode* pre = nullptr; 
        ListNode* cur = head; 

        for (; n >= k; n -= k) {
            for (ll i = 0; i < k; i++) {
                ListNode* next = cur->next;  
                cur->next = pre; 
                pre = cur; 
                cur = next; 
            } 
            ListNode* next = p0->next; 
            p0->next->next = cur; 
            p0->next = pre; 
            p0 = next; 
        }  
        return dummy->next; 
    }
}; 

// 递归
class Solution {
12public: 
13    ListNode* reverseList(ListNode* head) {
14        if (head == nullptr || head->next == nullptr) {
15            return head; 
16        } 
17        ListNode* newhead = reverseList(head->next); 
18        head->next->next = head; 
19        head->next = nullptr; 
20        return newhead; 
21    }
22    ListNode* reverseKGroup(ListNode* head, int k) {  
23        if (k == 1) {
24            return head; 
25        }
26        ListNode* curr = head; 
27        for (int i = 0; i < k - 1; i++) {
28            if (curr == nullptr) {
29                return head; 
30            } 
31            curr = curr->next; 
32        }     
33        if (curr == nullptr) {
34            return head; 
35        }
36        ListNode* next = curr->next; 
37        curr->next = nullptr; 
38        ListNode* newhead = reverseList(head);  
39        head->next = reverseKGroup(next, k); 
40        return newhead; 
41    }
42};
```

## 本日遇到的问题

1. 对于学习过的算法运用不够熟练, 在做算法题目的时候不能准确判断出应该使用哪一种算法 
1. 对缩放视图的操作掌握不够熟练, 不能独立写出

## 明日学习计划

1. 对学习的UI的知识点梳理, 并对掌握不清的内容多进行练习 
1. 折叠cell的原理及实现练习

