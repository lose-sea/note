## 本日学习内容

1. 特性指示符(assing, atomic, nonatomic, strong, weak) 
1. 类的多态的深入了解(父类对象赋给子类指针变量, 子类对象赋给父类指针变量, 编译期和运行期的差异,对象的实际指向与方法调用的差异)  
1. isEqual 和 ==(重写isEqual)

## 今日算法题

## 题目1:  [454. 四数相加 II](https://leetcode.cn/problems/4sum-ii/)  

![image-20260318210058051](/Users/lose_sea/Library/Application Support/typora-user-images/image-20260318210058051.png)

```objc
class Solution {
public:
    int fourSumCount(vector<int>& nums1, vector<int>& nums2, vector<int>& nums3, vector<int>& nums4) {
        unordered_map<int, int> mp; 
        for (int i = 0; i < nums1.size(); i++) {
            for (int j = 0; j < nums2.size(); j++) {
                mp[nums1[i] + nums2[j]]++; 
            }
        } 
        int result = 0; 
        for (int i = 0; i < nums3.size(); i++) {
            for (int j = 0; j < nums4.size(); j++) {
                result += mp[0 - nums3[i] - nums4[j]]; 
            } 
        } 
        return result; 
    }
};
```



## 本日遇到的问题

1. 对于通过stringWithString方式创建的字符串的存储位置理解不清楚

## 明日学习计划

1. 根据学长的博客回顾之前理解不够清楚的知识点
1. isa 和 superclass 分析 
1. isKindOfclass 和 isMemberOfclass 方法