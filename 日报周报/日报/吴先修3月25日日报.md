## 本日学习内容

1. NSArray基础方法练习(使用枚举器遍历NSArray, (objectEnumerator, reverseObjectEnumerator, nextObject, allObjects), 排序)
1. NSSet 基础方法练习 (创建, 判断交集, 子集, 过滤, 判断元素重复的标准, 重写hash方法)  

## 今日算法题

## 题目1: [42. 接雨水](https://leetcode.cn/problems/trapping-rain-water/) 

![image-20260325212435178](/Users/lose_sea/Library/Application Support/typora-user-images/image-20260325212435178.png)

```objc
// 双指针
class Solution {
public:
    int trap(vector<int>& height) {
        if (height.size() <= 2) {
            return 0; 
        } 
        vector<int> maxleft(height.size(), 0); 
        vector<int> maxright(height.size(), 0); 
        
        maxleft[0] = height[0]; 
        for (int i = 1; i < height.size(); i++) {
            maxleft[i] = max(height[i], maxleft[i - 1]); 
        } 
        maxright[height.size() - 1] = height[height.size() - 1]; 
        for (int i = height.size() - 2; i >= 0; i--) {
            maxright[i] = max(height[i], maxright[i + 1]); 
        } 

        int sum = 0; 
        for (int i = 1; i < height.size() - 1; i++) {
            int count = min(maxleft[i], maxright[i]) - height[i]; 
            if (count > 0) {
                sum += count; 
            }
        } 
        return sum; 
    }
};  

//单调栈 
class Solution {
public:
    int trap(vector<int>& height) {
        stack<int> sk;  
        sk.push(0);  
        int sum = 0; 
        for (int i = 1; i < height.size(); i++) {
            while (!sk.empty() && height[i] > height[sk.top()]) {
                int mid = sk.top(); 
                sk.pop(); 
                if (!sk.empty()) {
                    int h = min(height[sk.top()], height[i]) - height[mid]; 
                    int w = i - sk.top() - 1;  
                    sum += h * w; 
                }
            } 
            sk.push(i); 
        }  
        return sum; 
    }
};
```



## 本日遇到的问题

1. NSArray 枚举器遍历 
1. 对集合整体调用方法

## 明日学习计划

1. NSMutableSet基础方法练习 
1. NSDictionary 基础方法练习
