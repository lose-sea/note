 /Users/lose_sea/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_14iqkiv1lroq22_e4

 cc/temp/RWTemp/2026-09/b00993d7e2843d5d4e95bb2fcd466567/38bebfbacbeb0cdb01a336e99a9742ec.jpg

 我的PlayerDetailShow想要实现类似的布局,中间是songCover,内部的cover最好可以旋转,

 然后有一个需要显示歌词,等我实现网络之后要实现歌词根据歌曲播放然后自动换行,

 下方有一个进度条可以滑动来控制歌曲播放, 然后下方几个按钮,喜欢按钮, 评论按钮,上一首,播放,下一首 





现在我想要完成菜单界面, 上面是头像右边是昵称和账号, 然后下面是几个列表,底部有几个按钮,设置, 夜间模式,这个按钮可以太阳和月亮图标转换,先画出来吧





问题一：最初编写闰年判断函数时，只考虑了“能被 4 整除但不能被 100 整除”这
一条件，遗漏了“能被 400 整除”的情况。导致测试数据“2000 2”输出了 28 而非 29。
经过查阅闰年定义，发现世纪年（整百年）必须能被 400 整除才是闰年，于是补充了
elif year % 400 == 0 的判断分支，问题得到解决。
问题二：最初将所有逻辑写在主程序中，没有使用函数封装，导致代码结构混
乱。后来按照模块化编程思想，将闰年判断和天数计算分别抽取为 isLeapYear 和
daysOfMonth 两个函数，主程序只负责输入输出和分支判断，代码层次变得清晰，也
便于后续调试和修改。
问题三：在判断月份合法性时，最初写成了 if mon > 1 and mon < 13，虽然逻辑正
确，但后来发现更简洁的写法是 if 0 < mon < 13，利用 Python 支持的链式比较，使代
码更加简洁易读。





通过本次实验,我深刻体会到了分支结构在实际问题中的应用｡在处理多条件判 断时,合理使用if-elif-else 结构可以让程序逻辑清晰且不易出错｡同时,将复杂问题 分解为若干小问题､用函数封装的方式来解决,是一种非常重要的编程思想,不仅提 高了代码的可读性,也方便了后续的维护和测试｡在调试过程中,我学会了通过构造 不同的测试用例(包括正常情况和边界情况)来验证程序的正确性,这种测试思维对 今后的编程学习具有重要的指导意义｡



本实验将问题分解为两个子问题：闰年判断和月份天数计算，分别封装为独立函 数，提高代码的可读性和可维护性。具体步骤如下： 第一步：定义 isLeapYear(year)函数，用于判断给定年份是否为闰年。函数内部使 用 if-elif-else 分 支 结 构 ， 按 照 闰 年 判 断 标 准 进 行 条 件 判 断 ， 返 回 布 尔 值 True 或 False。 第二步：定义 daysOfMonth(isLeap, month)函数，根据月份参数返回对应天数。函 数内部通过分支结构将月份分为三类：31 天的月份、30 天的月份、以及需要额外判断 闰年的 2 月。 第三步：在主程序中，使用 input()函数读取用户输入，通过 split()方法将输入按 空格分割，再用 map(int, ...)将字符串转换为整数 year 和 mon。 第四步：使用分支结构判断月份是否在 1~12 的合法范围内。若合法，调用 daysOfMonth 函数计算并输出天数；若不合法，输出 Error。
