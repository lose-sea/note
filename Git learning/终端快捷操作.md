### 1. 清空当前整行

如果不小心把一大段命令和注释都粘进来了，直接按：

- **Ctrl + C**：直接放弃当前行，清空并换行，相当于 “取消”。
- 或者 **Ctrl + U**：从光标位置向前删除到行首，整行内容瞬间清空。

### 2. 按词 / 按段删除

如果不想全删，只删一部分：

- **Ctrl + W**：向前删除一个单词（以空格为分隔）。
- **Ctrl + K**：从光标位置向后删除到行尾。