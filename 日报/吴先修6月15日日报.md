# 6月15日日报

## 本日学习内容

1. 网易云音乐的仿写 
2. 抽屉视图的尝试实现 (ContainViewController, MainViewController, MenuViewController)

## 今日算法题

### 题目1: [77. 组合](https://leetcode.cn/problems/combinations/) 

![image-20260615210916719](/Users/lose_sea/Desktop/pintures/image-20260615210916719.png)

```objc
class Solution {
public: 
    vector<vector<int>> result; 
    vector<int> path;  

    void backtracking(int n, int k, int startIndex) {
        if (path.size() == k) {
            result.push_back(path); 
            return; 
        } 
        for (int i = startIndex; i <= n - (k - path.size()) + 1; i++) {
            path.push_back(i); 
            backtracking(n, k, i + 1); 
            path.pop_back(); 
        } 
    }

    vector<vector<int>> combine(int n, int k) {  
        result.clear(); 
        path.clear(); 
        backtracking(n, k, 1); 
        return result; 
    }
};
```

## 本日遇到的问题

1. 在 init 方法中想要获取屏幕的宽度, 使用`[UIScreen mainScreen].bounds.size.width`方法但是已经弃用, 通过 ` _*menuWidth = self.view.window.windowScene.screen.bounds.size.width`方法获取到的值为 0 
2. 在打开和关闭抽屉视图的方法中, 使用Masonry 约束然后调用` [self.view layoutIfNeed]`的方式但是没有用, 抽屉视图并没有显示  
3. 通过`animateWithDuration: animations:`方法设置抽屉视图的弹出和收起, 在其中是使用Masonry 约束不会显示动画, 直接调整frame可以显示动画

## 明日学习计划

1. 网易云仿写
2. 在仿写界面的过程中回顾复习自己之前学习过的掌握不过熟练的知识点

