## 本日学习内容

1. 详细回顾NSString 和 NSMutableString的相关方法 (创建,追加,删除, 替换, 删除) 
1. copy 和 mutableCopy 的原理和实现, 重写copyWithZone  和 copyWithZone 方法 
1. 浅复制和深复制的区别  
1. 手动内存管理(对象的引用计数, 自动释放池) 

## 今日算法题

## 题目: [206. 反转链表 ](https://leetcode.cn/problems/reverse-linked-list/)

![image-20260315214502733](/Users/lose_sea/Library/Application Support/typora-user-images/image-20260315214502733.png)

```objc
// 迭代
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        if (head == nullptr || head->next == nullptr) {
            return head; 
        }   
        ListNode* prev = NULL; 
        ListNode* curr = head;  
        while (curr) { 
            ListNode* next = curr->next; 
            curr->next = prev;   
            prev = curr;    
            curr = next; 
        }  
        return prev; 
    }
};  
// 递归
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        if (head == nullptr || head->next == nullptr) {
            return head; 
        }   
        ListNode* newhead = reverseList(head->next); 
        head->next->next = head; 
        head->next = nullptr;  
        return newhead; 
    }
};  
// 头插
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        if (head == nullptr || head->next == nullptr) {
            return head; 
        }   
        ListNode* dummy = new ListNode(0); 
        ListNode* curr = head; 
        while (curr) {
            ListNode* next = curr->next; 
            curr->next = dummy->next; 
            dummy->next =  curr; 
            curr = next; 
        } 
        return dummy->next; 
    }
}; 
// 栈
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        if (head == nullptr || head->next == nullptr) {
            return head; 
        }   
        stack<ListNode*> st; 
        ListNode* curr = head;  
        while (curr) {
            st.push(curr); 
            curr = curr->next; 
        } 
        ListNode* dummy = new ListNode(0);  
        curr = dummy; 
        while (!st.empty()) {
            curr->next = st.top(); 
            st.pop();  
            curr = curr->next; 
        }  
        curr->next = nullptr; 
        return dummy->next; 
    }
}; 
```



## 今日遇到的问题

1. 对于算法的练习有所懈怠, 不能正确写出反转链表

## 明日学习计划

1. 回顾NSArray的相关方法(创建, 查找, 插入, 截取, 对集合整体调用方法, 对集合元素进行排序) 

2. 类, 元类的关系

   ​    