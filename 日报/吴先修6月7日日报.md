# 6月7日日报

## 本日学习内容

1. share 仿写 
2. 实现了修改密码界面布局和功能实现 
3. 对登录界面,注册界面和修改密码界面添加了防爆处理,(账号密码不能为空, 密码由6 - 10 位数字和字母组成) 
4. 对登录界面和注册界面logo图片和背景颜色不一致的问题进行处理解决 
5. 对之前写的文章分类界面没有做滚动视图进行了重写,将tableView 添加到滚动视图,与segmentedControl 进行联动切换

## 今日算法题

### 题目1:  [236. 二叉树的最近公共祖先](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/) 

![image-20260607203927788](/Users/lose_sea/Desktop/pintures/image-20260607203927788.png)

```objc
class Solution {
public:
    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) { 
        if (root == p || root == q || root == nullptr) {
            return root; 
        }
        TreeNode* left = lowestCommonAncestor(root->left, p, q); 
        TreeNode* right = lowestCommonAncestor(root->right, p, q); 

        if (left != nullptr && right != nullptr) {
            return root; 
        }
        if (left != nullptr && right == nullptr) {
            return left; 
        } else if (left == nullptr && right != nullptr) {
            return right; 
        } else {
            return nullptr; 
        } 
    }
};
```

## 本日遇到的问题

1. 在注册界面的密码设置的时候,设置textField.secureTextEntry = YES, 会导致通过虚拟键盘输入一个字符后输入框卡住, 无法继续输入, 但是设置为textField.secureTextEntry = NO; 就不会导致此问题
1. 搜索界面通过点击搜索按钮跳转其他界面会有一瞬间的卡顿, 导致界面内容重叠(前一个界面的内容还没有消失, 后一个界面的内容已经出现)

## 明日学习计划

1. share仿写
2. 在仿写界面的过程中回顾复习自己之前学习过的掌握不过熟练的知识点

