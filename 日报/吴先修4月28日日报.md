# 4月28日日报

## 本日学习内容

1. 

### 题目1: [148. 排序链表](https://leetcode.cn/problems/sort-list/) 

![image-20260428182452389](/Users/lose_sea/Desktop/pintures/image-20260428182452389.png)

```objc
define ll long long  
    // 复制到数组然后排序
// class Solution {
// public:
//     ListNode* sortList(ListNode* head) {
//         vector<ll> list; 
//         ListNode* curr = head; 
//         while (curr != nullptr) {
//             list.push_back(curr->val);  
//             curr = curr->next; 
//         } 
//         sort(list.begin(), list.end()); 
//         curr = head;     
//         for (ll i = 0; i < list.size(); i++) {
//             curr->val = list[i]; 
//             curr = curr->next; 
//         } 
//         return head; 
//     }
// };

// 归并排序
#define ll long long 
class Solution {
public:
    ListNode* sortList(ListNode* head) {
        return sortList(head, nullptr); 
    } 
    ListNode* sortList(ListNode* head, ListNode* tail) {
        if (head == nullptr) {
            return head; 
        } 
        if (head->next == tail) {
            head->next = nullptr; 
            return head; 
        }
        ListNode* slow = head; 
        ListNode* fast = head; 
        while (fast != tail) {
            slow = slow->next; 
            fast = fast->next;   
            if (fast != tail) {
                fast = fast->next;  
            }
        } 
        ListNode* mid = slow; 
        return merge(sortList(head, mid), sortList(mid, tail)); 
    } 

    ListNode* merge(ListNode* l1, ListNode* l2) {
        if (l1 == nullptr) {
            return l2; 
        } 
        if (l2 == nullptr) {
            return l1; 
        } 
        ListNode* dummy = new ListNode(0); 
        ListNode* curr = dummy; 
        while (l1 && l2) {
            if (l1->val <= l2->val) {
                curr->next = l1; 
                l1 = l1->next; 
            } else {
                curr->next = l2; 
                l2 = l2->next; 
            } 
            curr = curr->next; 
        }  
        if (l1 == nullptr) {
            curr->next = l2; 
        } else {
            curr->next = l1; 
        }
        return dummy->next; 
    }
};
```

## 本日遇到的问题

1. 对于学习过的算法运用不够熟练, 在做算法题目的时候不能准确判断出应该使用哪一种算法 
1. 对缩放视图的操作掌握不够熟练, 不能独立写出

## 明日学习计划

1. 对学习的UI的知识点梳理, 并对掌握不清的内容多进行练习 
1. 折叠cell的原理及实现练习

