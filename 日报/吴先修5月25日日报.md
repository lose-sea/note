# 5月25日日报

## 本日学习内容

1. share 仿写 
2. 了解了 UISearchController 的基本操作, (创建, 添加, 设置代理, 搜索框内容发生变化时调用`- (void) updateSearchResultsForSearchController:(UISearchController *)searchController` 和点击搜索按钮时调用`- (void)searchBarSearchButtonClicked:(UISearchBar *)searchBar`)方法
3. 使用 UICollectionView 的自定义cell实现了搜索页面的标签筛选



## 今日算法题

### 题目1:  [501. 二叉搜索树中的众数](https://leetcode.cn/problems/find-mode-in-binary-search-tree/) 

![image-20260525214715129](/Users/lose_sea/Desktop/pintures/image-20260525214715129.png)

```objc
class Solution {
public:
    void searchBST(TreeNode* root, unordered_map<int, int>& map) {
        if (root == nullptr) {
            return; 
        }
        map[root->val]++; 
        searchBST(root->left, map); 
        searchBST(root->right, map); 
    } 
    bool static cmp(const pair<int, int>& e1, const pair<int, int>& e2) {
        return e1.second > e2.second; 
    }
    vector<int> findMode(TreeNode* root) { 
        vector<int> result; 
        unordered_map<int, int> map; 
        if (root == nullptr) {
            return result; 
        }
        searchBST(root, map); 
        vector<pair<int, int>> vec(map.begin(), map.end()); 
        sort(vec.begin(), vec.end(), cmp); 
        int len = vec.front().second; 
        for (int i = 0; i < vec.size(); i++) {
            if (vec[i].second == len) {
                result.push_back(vec[i].first); 
            } else {
                break; 
            }
        } 
        return result; 
    }
```

## 本日遇到的问题

1. 对于 UISearchController 的代理方法理解不清

## 明日学习计划

1. share仿写
2. 在仿写界面的过程中回顾复习自己之前学习过的掌握不过熟练的知识点

