# 5月24日日报

## 本日学习内容

1. share 仿写 
2. 解决了开启界面UIImageView没有填充屏幕的问题
3. 解决了在首页的tableView中, 第一个cell使用了自定义cell, 设置了无限轮播视图, 但是在滑动tableView的时候, 第一个cell的定时器会关闭的问题
4. 了解了 UISearchController 控件(创建, 添加到导航栏, 代理方法的实现)



## 今日算法题

### 题目1: [530. 二叉搜索树的最小绝对差](https://leetcode.cn/problems/minimum-absolute-difference-in-bst/) 

![image-20260524215338772](/Users/lose_sea/Desktop/pintures/image-20260524215338772.png)

```objc
class Solution {
public: 
    int result = INT_MAX; 
    TreeNode* pre = nullptr; 
    void traversal(TreeNode* root) {
        if (root == nullptr) {
            return; 
        } 
        traversal(root->left); 
        if (pre != nullptr)  {
            int curr = abs(root->val - pre->val); 
            if (curr < result) {
            result = curr; 
        } 
        }
        pre = root; 
        traversal(root->right); 
    }

    int getMinimumDifference(TreeNode* root) {
        traversal(root); 
        return result; 
    }
};
```

## 本日遇到的问题

1. 在首页的tableView中, 第一个cell使用了自定义cell, 设置了无限轮播视图, 但是在滑动tableView的时候, 第一个cell的定时器会关闭 
2. 不理解UISearchController 的代理方法, 以及 UISearchController 和 UISearchBar 的区别

## 明日学习计划

1. share仿写
2. 在仿写界面的过程中回顾复习自己之前学习过的掌握不过熟练的知识点

