# 5月31日日报

## 本日学习内容

1. share 仿写 
2. 实现了个人作品上传界面的布局
3. 实现TableView 滑动和 UISegmentedControl 的联动

## 今日算法题

### 题目1:  [617. 合并二叉树](https://leetcode.cn/problems/merge-two-binary-trees/) 

<img src="/Users/lose_sea/Desktop/pintures/image-20260601213000544.png" alt="image-20260601213000544" style="zoom:67%;" />

```objc

class Solution {
public:
    TreeNode* traversal(TreeNode* root1, TreeNode* root2) {
        if (root1 == nullptr) {
            return root2; 
        } 
        if (root2 == nullptr) {
            return root1; 
        } 
        TreeNode* root = new TreeNode(root1->val + root2->val); 
        root->left = traversal(root1->left, root2->left); 
        root->right = traversal(root1->right, root2->right); 

        return root; 
    }
    TreeNode* mergeTrees(TreeNode* root1, TreeNode* root2) {
        if (root1 == nullptr) {
            return root2; 
        } 
        if (root2 == nullptr) {
            return root1; 
        } 
        return traversal(root1, root2); 
    }
};
```

## 本日遇到的问题

1. 在实现上传文章界面的时候, 需要添加3个可以滚动的tableView, 在 view 中定义一个 UIScrollVIew 和三个 UITableVie, 将UITableView添加到scrollView, 但是不显示, 通过调试在view 中scrollView 的size 为 0,使用 layoutIfNeeded 也没有用, 最后不采用Masonry, 通过直接赋值的方式解决

## 明日学习计划

1. share仿写
2. 实现文章分类界面 UITableView 的滑动功能
3. 在仿写界面的过程中回顾复习自己之前学习过的掌握不过熟练的知识点

