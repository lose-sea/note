# 5月21日日报

## 本日学习内容

1. share 仿写 
2. 实现首页顶部滚动视图的添加(自定义cell, 设置UIScrollView, 添加iView至UIScrollView)

## 今日算法题

### 题目1:  [700. 二叉搜索树中的搜索](https://leetcode.cn/problems/search-in-a-binary-search-tree/) 

<img src="/Users/lose_sea/Desktop/pintures/image-20260521212130472.png" alt="image-20260521212130472" style="zoom: 50%;" />

```objc

class Solution {
public:

    // 中序遍历 
    bool judge = true; 
    TreeNode* pre = nullptr; 
    void traversal(TreeNode* root) {
        if (judge == false) {
            return; 
        } 
        if (root == nullptr) {
            return; 
        }
        traversal(root->left); 
        if (pre != nullptr && root->val <= pre->val) {
            judge = false; 
            return; 
        } 
        pre = root; 
        traversal(root->right); 
    }
    bool isValidBST(TreeNode* root) {
        if (root == nullptr) {
            return true; 
        } 
        traversal(root);
        return judge; 
    }
};
```

## 本日遇到的问题

1. 在自定义cell中, 添加UIScrollView, 在往UIScrollView添加iView时布局出现混乱(设置UIScrollView.contentSize使用cell.contentView.bounds.size.width * images.count) 设置,iView添加到scrollView中, 设置了按页滑动, 但是图片还是没有按页显示 
2. 使用MVC时不能处理好MVCview层和controller中的方法处理,在使用自定义cell时先将tableView添加到view,再将view添加到controller的view, 或者这在controller加入tableview 

## 明日学习计划

1. share仿写
2. 在仿写界面的过程中回顾复习自己之前学习过的掌握不过熟练的知识点

