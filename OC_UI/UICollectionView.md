# UICollectionView





> **FlowLayout（流式布局）** 是一种自动换行的布局方式，核心是**子元素按顺序排列，一行排满后自动换到下一行 / 列**，适配容器尺寸变化，常用于标签、按钮、卡片等场景Oracle。
>
> ### 一、核心原理
>
> + **排列规则**：子元素从起始位置开始，沿主方向（水平 / 垂直）依次排列，超出容器边界时自动换行 / 列Oracle。
> + **自适应**：容器大小改变时，子元素自动重排，无需手动计算位置。
> + **灵活性**：支持不同尺寸的子元素混合排列，支持对齐、间距配置。