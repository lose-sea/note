# 数据容器

一种可以容纳多份数据的数据类型 (容器), 容纳的每一份数据称之为 1 个元素, 每一个元素都可以是任意类型的数据, 如字符串, 数字, 布尔

# 列表

列表是数据容器中的一类, 是一次性可以存储多个数据(元素)的

定义: 

```python
列表名称 = [元素1, 元素2, ...] 

s = [1, 2, 3, 4]
```

特点: 

1. 可以存储不同类型的元素
2. 元素有序 (根据添加时候的顺序) , 可以重复, 元素可以修改

可以根据索引来访问列表中的元素, 正向索引从0开始, 反向索引从 -1 开始, 如果索引是负数, 说明是反向访问

![image-20260322132411745](C:/Users/Lenovo/AppData/Roaming/Typora/typora-user-images/image-20260322132411745.png)

##  基本操作

```python

s = [1, 2, 3, 4, 5, 6, 7]
print(s[-4])    # 4
print(type(s)) # <class 'list'> 

# 修改
s[5] = 'hello' 
print(s[5]) # hello 

# 打印列表
print(s) # [1, 2, 3, 4, 5, 'hello', 7] 
for i in s : 
    print(i, end = " ")  # 1 2 3 4 5 hello 7 
print()
# 删除  del
del s[3] 
print(s) # [1, 2, 3, 5, 'hello', 7]
```

可以用`print (列表名)` 直接打印整个列表 

## 切片

切片是指对操作的数据截取其中一部分的操作, 列表, 字符串, 元组都支持切片操作

### 语法: 

```python
序列数据[开始索引: 结束索引: 步长]
```

1. **不包含结束索引位置对应的元素** (开始索引未指定默认为 0; 结束索引未指定默认为列表长度(直到列表末尾); 步长未指定默认为 1 ) 
2. 索引采用正向, 反向索引都可以 
3. 步长是选取间隔, 默认步长为 1 

## 列表常见方法

| 方法      | 作用                                                         | 样例               |
| --------- | ------------------------------------------------------------ | ------------------ |
| append()  | 在列表的尾部追加元素                                         | s.append(10086)    |
| insert()  | 在指定索引之前，插入该元素                                   | s.insert(0, 92)    |
| remove()  | 移除列表中第一个匹配到的值                                   | s.remove(75)       |
| pop()     | 删除列表中指定索引位置的元素（如果未指定索引，默认删最后一个） | s.pop(2) / s.pop() |
| sort()    | 对列表进行排序（列表元素的数据类型一致，才可以进行排序）     | s.sort()           |
| reverse() | 反转列表元素                                                 | s.reverse()        |

**sort排序要求所以的元素都是同一类型**

```python
s = [45, 56, 98, 5, 35]  
s.sort()
print(s)  #[5, 35, 45, 56, 98]
```

#### sum(列表名) 求和

#### len(列表名) 求元素个数

#### 合并列表

```python
# 合并两个列表, 并对合并后的列表去重
num1 = [12, 56, 87, 99, 87] 
num2 = [12, 99, 556, 36, 85, 87] 

print(num1) 
print(num2) 

for i in num1: 
    num2.append(i) 
print(num2)  # [12, 99, 556, 36, 85, 87, 12, 56, 87, 99, 87]  未去重

num_list = []  # 记录去重后的列表元素
 
for i in num2: 
    if i  not in num_list:  # in 判断元素是否存在于列表中, 存在返回 True, 否则返回 Flase 
        # not 取反
        num_list.append(i) 

print(num_list) # [12, 99, 556, 36, 85, 87, 56]
```

也可以直接用 + 将两个列表合并

```python
num1 = [12, 56, 87, 99, 87] 
num2 = [12, 99, 556, 36, 85, 87] 

num_list = num1 + num2 

print(num_list)
```

### in 运算符, 判断元素是否在列表中 

```python
元素 in list
```

判断元素是否存在于列表中, 存在返回 True, 否则返回 Flase 

## 解包 组包

解包: 将列表这一类容器解开成一个独立的元素 

组包: 将多个值合并到一个容器

```python
num1 = [12, 56, 87, 99, 87] 
num2 = [12, 99, 556, 36, 85, 87] 

num_list = [*num1, *num2]   # 解包

print(num_list)  # [12, 56, 87, 99, 87, 12, 99, 556, 36, 85, 87]
```

### 案例: 生成1 - 20 的平方组成的列表

#### 法一: 传统方法

```python
法一: 
num_list = [] 
for i in range(1, 21): 
    num_list.append(i ** 2) 
print(num_list)
```

#### 法二: 列表推导式

格式: 

```python
[要插入的值 for i in list] 
```

```py
num_list = [] 
num_list = [i ** 2 for i in range(1, 21)]
print(num_list)
```



### 案例: 生成1 - 20 所有偶数平方的列表 

#### 列表推导式:

```python
num_list = [] 
num_list = [i ** 2 for i in range(1, 21) if i % 2 == 0] 
print(num_list) # [4, 16, 36, 64, 100, 144, 196, 256, 324, 400]
```



# 字符串

字符串是字符的容器, 一个字符串中可以存放任意数量的字符 

特点: **不可变性 (无法修改)**, 有序性, 可迭代性 

字符串中每一个字符元素都有具有其对应的下标 (索引), 通过元素对应的索引, 就可以获取对应的元素 

> 从前向后,(正向索引), 下标从 0 开始, 从后向前 (反向索引), 下标从 -1 开始

# 切片

语法: `序列对象[开始索引: 结束索引: 步长]` 

```python
s = "hello world" 

print(s[4]) 
print(s[1: 6: 2]) 
```

如果是从 0 开始或者步长为 1 , 可以省略开始索引和步长 

```python
s = "hello world" 

print(s[: 5: ]) 
print(s[1: 6: ])
```

**步长为负数表示从后往前截取**

```python
s = "hello world" 
print(s[6: 0: -1]) # w olle
```

**反向截取的时候要注索引和结束索引也要从后往前**

### 字符串是不可修改的



## 常用方法

| 方法         | 作用                                                         | 样例                     |
| ------------ | ------------------------------------------------------------ | ------------------------ |
| find()       | 在字符串中查找子串，返回第一次出现的索引位置，找不到返回 - 1 | s.find('Python')         |
| count()      | 统计子串在字符串中出现的次数                                 | s.count('H')             |
| upper()      | 将字符串中的所有字母转换为大写                               | s.upper()                |
| lower()      | 将字符串中的所有字母转换为小写                               | s.lower()                |
| split()      | 将字符串按指定分隔符分割成列表                               | s.split(' ')             |
| strip()      | 去除字符串两端的空白字符或指定字符                           | s.strip() / s.strip('*') |
| replace()    | 将字符串中的指定子串替换为新的子串                           | s.replace('H', 'C')      |
| startswith() | 检查字符串是否以指定子串开头，返回布尔值                     | s.startswith('P')        |

# 元组(tuple)

列表特点: 元素可重复, 有序, 可以修改

如果需要记录一些信息, 而且不能修改, 只能查询, 就可以考虑使用元组: 

**元组一旦定义完成, 不可修改**

## 介绍

元组是不可变的序列, 类似于列表, 但创建后不能修改

## 特点

1. 可以存储不同类型的元素 
2. 元素可以重复, 有序, **不可以修改** (支持索引访问, 切片)

## 定义

```python
元组名称 = (元素1, 元素2, ...) 

# 定义空元组
元组名称 = () 
元组名称 = tuple()
```

## 方法

1. `count() 统计某元素在元组中出现的次数`
2. `index() 查找某个元素在元组中索引位置 (第一次出现的位置)`



```python
t1 = (12, 23, 54, 54, 5)
print(type(t1)) # <class 'tuple'>
print(t1[-1]) # 5
```

可以正向访问, 也可以反向访问

**如果要定义单个元素的元组, 单个元素之后需要加上逗号 , **

```python
t = (100) 
print(type(t))  # <class 'int'>
t1 = (100, ) 
print(type(t1)) # <class 'tuple'>
```

## 组包与解包

+ 组包 (Packing): 将多个值合并到一个容器 (元组, 列表) 中 
+ 解包 (Unpacking): 将容器 (元组, 列表) 解开成独立的元素, 分别赋值给多个变量

```python
# 定义元组, 组包
t1 = (5, 7, 9, 1) 
t2 = 5, 7, 9, 1


# 基础解包
a, b, c, d = t1 


# ( * ) 扩展解包
x, *y, z = t2   # x 为 5, y 为 [7, 9] z 为 1
s, *o = t2      # s 为 5, o 为 [7, 9, 1]
*o, e = t2      # o 为 [5, 7, 9], e 为 1
```

**在元组解包时, * 表示收集剩余元素, 允许我们处理不确定数量的元素 (生成列表, 以便可以进行进一步的处理) ** 



通过组包和解包交换两个变量的值

```python
a = 10 
b = 20 
a, b = b, a  
print(a) # 20
print(b) # 10
```

## 案例

```python
# 保存学生信息
students = (
    ("S001", "王林", 85, 92, 78),
    ("S002", "李慕婉", 92, 88, 95),
    ("S003", "十三", 78, 85, 82),
    ("S004", "曾牛", 88, 79, 91),
    ("S005", "周软", 95, 96, 89),
    ("S006", "王卓", 76, 82, 77),
    ("S007", "红蝶", 89, 91, 94),
    ("S008", "徐立国", 75, 69, 82),
    ("S009", "许木", 86, 89, 98),
    ("S010", "通天", 66, 59, 72)
) 

print("学号\t 姓名 \t 语文 \t 数学 \t 英语 \t 总分 \t 平均分") 

## 方式一
# # 计算每个学生的总分, 各科平均分, 然后一并输出
# for s in students: 
#     total = s[2] + s[3] + s[4]  
#     ava = total / 3 
#     print(f"{s[0]} \t{s[1]}\t {s[2]} \t {s[3]} \t {s[4]} \t {total} \t {ava: .1f}") 


## 方式二: 元组解包 
for id, name, chinese, math, english in students: 
    total = chinese + math + english 
    avg = total / 3 
    print(f"{id} \t {name}\t {chinese} \t {math} \t {english} \t {total} \t {avg: .1f}")


    
# 统计各学科的最低分, 最高分, 平均分 
# 获取到各科的成绩列表 
chinese_score = [s[2] for s in students] 
math_score = [s[3] for s in students] 
english_score = [s[4] for s in students] 

# 统计各科成绩的最高分, 最低分, 平均分
print(f"chinese min_score is {min(chinese_score)}, max_score is {max(chinese_score)}, avarage is {sum(chinese_score) / len(chinese_score)}") 
print(f"math min_score is {min(math_score)}, max_score is {max(math_score)}, avarage is {sum(math_score) / len(math_score)}") 
print(f"english min_score is {min(english_score)}, max_score is {max(english_score)}, avarage is {sum(english_score) / len(english_score)}") 


方法一: 
# # 查找成绩优秀 (平均分大于 90) 的学生 
# for s in students: 
#     total = s[2] + s[3] + s[4] 
#     avg = total / 3 
#     if avg > 90: 
#         print(f"学号: {s[0]}, 姓名: {s[1]}, 平均分: {avg: .1f}")


方法二: 
# 元组解包
for id, name, chinese, math, english in students:  
    total = chinese + math + english 
    avg = total / 3 
    if avg > 90: 
        print(f"学号: {id}, 总分: {total}, 平均分: {avg: .1f}")
```







# 集合 (set) 

集合是一种无序的, 不可重复, 可修改的数据容器 

空集合的定义不可以使用{}, {}表示的是空字典, 由于集合是无序的, 因此是不支持下标索引访问的

```python
s1 = {5, 3, 0, 6, 6, 3, 8, 4} 
print(s1) # {0, 3, 4, 5, 6, 8}
print(type(s1)) # <class 'set'>
```

## 集合的定义

```python
定义1: 集合名 = {元素1, 元素2, 元素3...}

定义2: 集合名 = set()
```

**空集合的定义不可以使用{}, {}表示的是空字典**



## 集合基本操作

| 操作           | 含义                                                         | 样例                |
| -------------- | ------------------------------------------------------------ | ------------------- |
| add(..)        | 添加元素到集合中                                             | s1.add('t')         |
| remove(..)     | 移除集合中的指定元素（指定元素不存在将报错）                 | s1.remove('t')      |
| pop()          | 随机删除集合中的元素并返回                                   | e = s1.pop()        |
| clear()        | 清空集合                                                     | s1.clear()          |
| difference()   | 求取两个集合的差集（包含在第一个集合但不包含在第二个集合的元素） | s1.difference(s2)   |
| union()        | 求取两个集合的并集                                           | s1.union(s2)        |
| intersection() | 求取两个集合的交集                                           | s1.intersection(s2) |



## 集合推导式

**快速构建集合**

### 语法

```python
{要往集合中添加的元素 for s in set1 if 条件}
```

