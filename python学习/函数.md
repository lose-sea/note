函数是组织好的, 可重复使用的, 用来实现特定功能的代码片段

## 函数的定义和调用

```python
# 定义函数
def 函数名 (参数列表): 
    函数体
    ......
    return 返回值

# 调用函数
函数名 (参数)
```

函数定义时的参数列表与返回值语句是可有可无的(有需求确定, 函数必须先定义后调用执行 (函数定义时, 内部代码并未执行, 在调用时才执行) 

### 函数使用的注意事项 

1. 函数必须先定义, 再调用
2. 函数定义时, 并不会执行, 只有再调用函数时, 函数体的逻辑才会运行 
3. 函数中通过缩进来描述归属关系