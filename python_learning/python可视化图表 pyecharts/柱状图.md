# 柱状图

## 基础柱状图



### 导入包

```python
# 导包
from pyecharts.charts import Bar
```



### 实例化柱状图对象 

```python
bar = Bar()
```

 

### 添加数据



```python
# 添加 x 轴数据
bar.add_xaxis(["中国", "美国", "英国"])

# 添加 y 轴数据
bar.add_yaxis("GDP", [30, 20, 10]) 
```



### 生成图像 

```python
bar.render() 
```

这样就完成了一个简单的柱状图



![image-20260818020416905](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20260818020416905.png)

### 其他设置 

#### 翻转 x 轴和 y 轴 

通过reversal_axis() 方法可以翻转 x 轴和 y 轴 

```python
bar.reversal_axis()
```

效果图: 

![屏幕截图 2026-08-18 020559](C:\Users\Lenovo\Pictures\Screenshots\屏幕截图 2026-08-18 020559.png)

#### 设置数值显示的位置

要实现这个功能还需要导入对应的包

```python
from pyecharts.options import LabelOpts
```

在传入 y 轴数据的时候设置

```python
bar.add_yaxis("GDP", [30, 20, 10],label_opts=LabelOpts(position="right"))
```

效果 

![image-20260818020845548](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20260818020845548.png)

## 基础时间线柱状图



### 时间线

Timeline() 时间线 

柱状图描述的是分类数据，回答的是每一个分类中『有多少？』这个问题。这是柱状图的主要特点，同时柱状图很难动态的描述一个趋势性的数据。这里 pyecharts 为我们提供了一种解决方案‑时间线 

如果说一个 Bar、Line 对象是一张图表的话，时间线就是创建一个一维的 x 轴，轴上每一个点就是一个图表对象



### 绘制 

#### 导入包 

相对于基础的柱状图,绘制时间线柱状图还需要导入 Timeline 包

```python
from pyecharts.charts import Bar, Timeline
from pyecharts.options import LabelOpts
```

####  准备数据

在时间线上的不同点, 需要展示不同的柱状图, 这就要准备多个柱状图

```python
bar1 = Bar()
bar1.add_xaxis(["中国", "美国", "英国"])
bar1.add_yaxis("GDP", [30, 30, 20], label_opts=LabelOpts(position="right"))
bar1.reversal_axis()

bar2 = Bar()
bar2.add_xaxis(["中国", "美国", "英国"])
bar2.add_yaxis("GDP", [50, 50, 50], label_opts=LabelOpts(position="right"))
bar2.reversal_axis()

bar3 = Bar()
bar3.add_xaxis(["中国", "美国", "英国"])
bar3.add_yaxis("GDP", [70, 60, 60], label_opts=LabelOpts(position="right"))
bar3.reversal_axis()
```



#### 创建时间线对象

```python 
timeline = Timeline()
```



#### 在时间线内添加柱状图对象

```python
timeline.add(bar1, "点1")
timeline.add(bar2, "点2")
timeline.add(bar3, "点3")
```



#### 绘图

**注意: 这里用时间线去绘图, 而不是某一个柱状图**

```python
# 绘图
# 这里用时间线去绘图, 而不是某一个柱状图
timeline.render("基础时间线柱状图.html")
```





效果 

![image-20260818022849043](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20260818022849043.png)



#### 设置自动播放 

```python
timeline.add_schema( 
    # 自动播放的时间间隔, 1000毫秒
    play_interval= 1000, 
    # 是否在自动播放的时候显示时间线
    is_timeline_show= True, 
    # 是否自动播放
    is_auto_play= True, 
    # 是否循环播放 
    is_loop_play= True
)
```



#### 设置主题

要实现主题设置需要导 ThemeType 包

```python
from pyecharts.globals import ThemeType
```

然后只需要在创建时间线对象的时候传入主题参数  



```python
timeline = Timeline(
    {"theme": ThemeType.LIGHT}
)
```

各种主题如下: 

| 编号                     | 颜色    | 备注                         |
| ------------------------ | ------- | ---------------------------- |
| ThemeType.WHITE          | 红蓝    | 默认颜色等同于 bar = Bar（） |
| ThemeType.LIGHT          | 蓝黄粉  | 高亮颜色                     |
| ThemeType.DARK           | 红蓝    | 黑色背景                     |
| ThemeType.CHALK          | 红蓝 绿 | 黑色背景                     |
| ThemeType.ESSOS          | 红黄    | 暖色系                       |
| ThemeType.INFOGRAPHIC    | 红蓝黄  | 偏亮                         |
| ThemeType.MACARONS       | 紫绿    |                              |
| ThemeType.PURPLE_PASSION | 粉紫    | 灰色背景                     |
| ThemeType.ROMA           | 红黑灰  | 偏暗                         |
| ThemeType.ROMANTIC       | 红粉蓝  | 淡黄色背景                   |